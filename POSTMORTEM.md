# 从多边形到比特币：一次带着两个错误诊断的完整复盘

> 指标构想由 [@XOX2707](https://github.com/XOX2707) 提出；数学推导、实现与调试由 Claude (Anthropic) 完成。
> 仓库：https://github.com/XOX2707/btc-neff ｜ 面板：https://xox2707.github.io/btc-neff/

一个晚上，从「三角形边数增加会趋近圆」这个直觉，做到一个每次运行 11 秒、自动写回仓库的实时指标。这篇记录推导、四个坑，以及我在过程中给出的两次错误诊断——后者可能比前者更有参考价值。

---

## 一、想法：把「光滑」变成一个可计算的数

三角形 → 八边形 → 圆，边越多越光滑。那能不能反过来问：**当前的比特币走势，相当于几边形？**

桥梁是周长比。正 N 边形外切于圆时，周长与圆周之比是

```
κ(N) = (N/π)·tan(π/N)      N → ∞ 时 κ → 1
```

小角展开得 κ ≈ 1 + π²/(3N²)，反解出**等效边数**：

```
N_eff = π / √(3(κ − 1))
```

把 κ 换成「实际价格路径长度 ÷ 平滑基准路径长度」，就得到一个连续的实时读数。κ=1.3 → N≈3.3（尖锐折线）；κ=1.05 → N≈8；κ=1.005 → N≈26（接近圆弧）。

## 二、两个容易被跳过的设计点

**归一化不是可选项。** 价格差分必须除以窗口内标准差，时间轴每根 K 线记 1。没有这一步，图表纵横比一变，「转角」就变了，边数毫无意义。这是整个指标能否成立的前提。

**只看边数会被噪声骗。** 锯齿和圆弧**都会**给出「很多条边」。区分它们的是转向一致性：

```
C = |Στ| / Σ|τ|      圆弧一直朝同一方向弯（C≈1），噪声来回折（C≈0）
```

所以状态判定必须同时用两者。合成数据验证：平滑正弦给出 `N_eff=11.16, C=1.00`，同幅度加噪给出 `N_eff=3.22, C=0.008`。区分度清晰。

顺带一提，N_eff 和 Kaufman 效率比、分形维数 D=2−H 属于同一族，**数学上不是新信息**。它赢在把「光滑度」变成「几边形」这个能直接想象的东西——赢在可解释性，不是赢在 alpha。

## 三、为什么跑在 GitHub Actions 上

定时器、运行环境、写回目标，三件事在同一处闭环，零服务器成本。脚本零依赖，只用 Python 标准库。

---

## 四、四个坑

### 坑 1：两次运行同时「凭空创建」同一个文件

第一次并发运行就炸了。日志：

```
CONFLICT (add/add): Merge conflict in data/neff_history.csv
CONFLICT (add/add): Merge conflict in data/neff_latest.json
error: could not apply f588a29...
push attempt 1 failed, retrying
error: Pulling is not possible because you have unmerged files.
push attempt 2 failed, retrying
...
Error: Process completed with exit code 1.
```

运行 #1 成功创建了 `data/` 并推送；运行 #2 在不知情的情况下**也从零创建了同名文件**。git 看到两个独立的 "add"，无法自动合并。

我原本的重试循环假设「推送失败 = 网络抖动」，重试三次。但真实的失败模式是冲突，rebase 停在冲突态后，后续每次重试都报 `Pulling is not possible`，空转到退出。

**修法不是改重试次数，是换语义。** 这些是**生成的产物**，冲突时没有「正确的合并」可言：

```bash
if git push origin "HEAD:$BR"; then exit 0; fi
# 推送被拒：丢弃本地提交，以远端为准，重新生成
git fetch origin "$BR"
git reset --hard "origin/$BR"
python neff.py          # CSV 追加到远端那份上，不可能再冲突
```

用两个克隆复现竞态验证过：B 被拒 → 重同步 → 重算 → 第 2 次推送成功，**且 A 的数据行没有丢失**。

### 坑 2：`actions/checkout` 检出的是触发时的 SHA

这是坑 1 的根因。`checkout` 默认检出**触发运行时的那个 commit**，对排队中的运行来说已经过时。运行 #2 检出在 `76b0579`（还没有 `data/`），而此时 main 已经是 `8b78042`。

```yaml
- uses: actions/checkout@<sha>
  with:
    ref: ${{ github.ref_name }}    # 检出分支最新，而非触发时的快照
    fetch-depth: 0
```

### 坑 3：仓库要求 action 锁定到完整 commit SHA

```
The actions actions/checkout@v7 and actions/setup-python@v7 are not allowed
because all actions must be pinned to a full-length commit SHA.
```

解法是查出真实 SHA 并写死。**这个值不能靠记忆写**，错一位工作流就起不来：

```yaml
- uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1      # v7.0.1
- uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
```

代价是升级要手动。注释里标版本号方便核对，Dependabot 认得这种写法。

### 坑 4（未解决）：`schedule` 从来没有触发过

仓库里 7 次运行，**全部是 `Manually run`**。试过三种 cron：`*/15`、错开到 `7,22,37,52`、改成 `0,30`。**一次都没触发。**

但 `workflow_dispatch` 每次都跑得通，11 秒完成。所以工作流文件在默认分支上、语法正确、权限也对——**配置本身没问题，是 schedule 事件根本没送达**。

GitHub 官方对定时任务只承诺 best-effort，高负载时会延迟甚至丢弃；也有说法是新建仓库的首次定时触发要等数小时才稳定。这是目前能给的最诚实的说法，**不是确诊**。

---

## 五、两次错误诊断

这一节是这篇复盘里我认为最值得看的部分。

### 错误一：把「数据停更」归因到 SHA 策略

数据停在某个时刻不再更新，我当时的判断是：定时运行因为 action 未锁定 SHA 而被策略拒绝。

**这是错的。** 被策略拒绝的运行**会留下一条失败记录**，而 Actions 列表里没有。真相是那次定时运行**根本没发生**。我把一个真实存在的问题（SHA 策略）和一个无关的现象（数据停更）错误地连了起来。

教训：**「当时确实存在另一个问题」不构成因果证据。** 该去看运行记录，而不是在已知问题里挑一个看起来能解释的。

### 错误二：「拥挤刻度」理论

`schedule` 不触发，我提出 `:00/:15/:30/:45` 是全平台最拥挤的时刻，建议错开到 `7,22,37,52`。

**随后被否证**：错开之后同样一次没触发。这个理论本身不是凭空来的，但**它没有被验证就被用来指导改动了**。改动无害，却消耗了一轮观察窗口——而每改一次 cron 都会重置观察。

教训：**在一个本来就难以观测的系统里，未经验证的理论驱动的改动会让后续判断更难做。** 正确的做法是先固定变量、拉长观察，而不是急着动手。

第三个理论我没有提。已知事实是：配置正确、`workflow_dispatch` 百分之百可用、`schedule` 零送达。到此为止。

---

## 五点五、一个自己找出来的估计偏差

写完上面那些之后，为了给人讲解原理，我去数值验证泰勒展开的适用范围——结果发现**指标本身在当前区间是有偏的**。

`N_eff = π/√(3(κ−1))` 来自 `tan x ≈ x + x³/3`，**只在 N 大时准**：

| N | κ 精确 | 二阶近似 | (κ−1) 相对误差 |
|---|---|---|---|
| 3 | 1.6540 | 1.3655 | **44.1%** |
| 5 | 1.1563 | 1.1316 | 15.8% |
| 12 | 1.0235 | 1.0228 | 2.7% |
| 50 | 1.0013 | 1.0013 | 0.16% |

而 BTC 的读数落在 3.5~5，**恰好是误差最大的区间**。实际影响是全线低估 7~13%。

修法：κ(N) 在 N>2 上严格单调递减，直接二分数值反解，不用渐进式。修完回归测试 N=3 还原成 3.0000、N=50 还原成 50.0000。

**这不只是精度问题。** 迁移历史数据时，4H 从 4.64 变成 5.05，**跨过了 N=5 的状态分界线，`sharp` 变成 `normal`**。多边形卡片画的形状也跟着变了。

因为每行都存了原始 κ，35 行历史可以精确重算，**没有留下口径断点**。这是当初把 κ 一起写进 CSV 的意外回报——当时只是觉得"中间量留着以防万一"。

同一轮验证还发现 **C 的零假设依赖窗口长度**：n=60 的随机游走下 E[C]≈0.0177，而实际读数在 0.003~0.048，也就是说**全部在随机游走的同一量级内**，其中两个还低于零假设。所以面板现在同时显示 `C/随机` 倍数——单看 C=0.02 这个数字对读者没有任何意义。

教训：**把中间量存下来，代价很小，回报是以后能无痛修正口径。** 以及，为了给别人讲清楚而去验证自己的推导，是发现自己错误最有效的方式。

## 六、顺带修掉的几个渲染缺陷

仪表盘写完后用真实数据在浅色/深色/390px 三种环境实拍验证，抓到四个：

- 移动端表格撑破页面（改成在卡片内横向滚动）
- 移动端图表文字缩到约 6px 不可读（窄屏换用独立 viewBox）
- 衰减图首点标签和 Y 轴刻度重叠成 `5.03.69`
- CSS 把 `5m` 大写成了 `5M`——**金融语境里 M 是百万，不能这么显示**

还有一处是我自己的说明文字写错：多边形按**四舍五入**取整，`round(3.57)=4`，但我一开始写成「3.57 画成三角形」。代码从来没那么做过。

---

## 七、它能做什么，不能做什么

**不能做的：** 它不预测方向。它是**滞后的描述量**，告诉你已经走完的路径长什么样。目前总共 25 行历史、零回测，这个阶段任何人（包括我）说它有 edge 都没有证据。

**能做的**，是换一层问题——不是「买不买」，而是「现在这个环境的几何形状是什么」：

- **执行层面**（唯一不需要预测对错就成立的用法）：N_eff 低意味着反转频繁，市价单反复付点差和滑点，挂被动单反而会被这些反转喂到成交。
- **止损尺度**：C 接近零时方向变化近似随机游走，止损放在噪声带里会被噪声打掉而非被趋势证伪打掉。σ/根 给出噪声的自然刻度。
- **解释「同一策略换个周期就是两种期望」**：把不同时间框架的 ER 并排放，这件事就被量化了。

一个实际读数的例子：某时刻 5m 的 ER=0.007、15m 的 ER=0.003，但 1H 的 ER=0.463。**短期完全停滞，中期有方向。** 而此时 1H 的 C 只有 0.0025——有净位移但转向毫无一致性，说明那个方向是跳跃式走出来的，不是平滑弧线。

**阈值是拍的，不是统计出来的。** `sharp<5`、`circular` 要 `N≥12 且 C>0.35` 这些分界线有直觉依据但没有分布检验，数据攒够后应该用实际分位数重新标定。

---

## 八、现状

自动采集链路已验证可用：OKX 五个时间框架全部抓取成功、`errors: {}`、提交回仓库、Pages 自动重新部署。唯一缺的是 `schedule` 的自动触发，手动触发始终可靠。

真正要看的不是任何单点数值，而是 **N_eff 随时间框架的衰减曲线如何变化**——多边形趋近圆本身就是个尺度极限，这条曲线等价于分形维数 D = 2−H。当那排多边形开始从左到右边数递增、最右边逐渐鼓成圆，同时 C 抬起来，那才是「走势圆化」的形态。

数据会自己攒。
